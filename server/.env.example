DATABASE_URL=postgres://localhost:5432/pricemate
PORT=4000
JWT_SECRET=change-me-in-production
ML_API_URL=http://localhost:8000
# Free key from https://gnews.io — powers the Prediction screen's news feed
# (server-side only, cached; never exposed to the app). Feed is disabled
# gracefully if unset.
GNEWS_API_KEY=
